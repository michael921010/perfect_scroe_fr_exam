import { styled } from "@mui/styles";
import MuiIcon from "@mui/icons-material/ArrowBackIosRounded";

const ArrowIcon = styled(MuiIcon)(({ theme }) => ({
  width: 25,
  heigth: 25,
}));

export default ArrowIcon;
